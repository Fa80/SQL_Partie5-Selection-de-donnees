--Exo1: Dans la table languages, afficher toutes les données de la table.--

--La commande SELECT permet d'afficher ou de selectionner des données d'une table.

SELECT * FROM `languges`;

-- La requête qui permet de sélectionner et d'afficher des données s'appelle SELECT.
-- Pour cela, il faut ajouter une clause à la commande SELECT  : la clause FROMqui définit de quelle structure (dans notre cas, une table) viennent les données.
